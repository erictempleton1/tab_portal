<!DOCTYPE html>
<html>
  <% include ../includes/index_header %>
  <body>
    <% include ../includes/index_navbar %>
    <div class="container-fluid">
      <div class="row">
          <% include ../includes/admin/admin_navbar_left %>
          <h4>Confirm Remove Site</h4>
          <p>Site name: <%= site.siteName %></p>
          <p>Created: <%= moment(site.createdDate ).format('MMM D, YYYY hh:mm') %></p>
          <p>Site Url: <%= site.siteUrl %></p>
          <p>Private: <%= site.isPrivate %></p>
          <form method="POST" action="/admin/sites/remove/<%= site.siteName %>">
              <input class="btn btn-default" type="submit" value="Remove Site">
          </form>
        </div>
      </div>
      <% include ../includes/index_footer %>
    </body>
</html>